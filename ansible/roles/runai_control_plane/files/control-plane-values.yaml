# Placeholder values file for the Run:ai control plane.
# The actual field names/required values depend on your Run:ai version/contract.
# Replace these with the values from the onboarding guide provided by Run:ai support.

global:
  domain: "runai.example.com"        # keep in sync with runai_control_plane_domain
  # imagePullSecret: <pull secret name provided by Run:ai>

# tenantName: "your-tenant-name"
# licenseKey: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
